{
  "name": "witch-journey-mcp",
  "version": "0.1.0",
  "private": false,
  "description": "Codex MCP server and in-game bridge for Witch's Apocalyptic Journey automation.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "witch-journey-mcp": "./server.mjs"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "npm run test:fake && npm run python-syntax-test && npm run script-syntax-test && npm run proof-script-help-test && npm run no-mouse-test",
    "test:fake": "npm run selftest && npm run calltest && npm run orchestration-test && npm run state-advance-policy-test && npm run bridge-sync-test && npm run bridge-sync-lock-test && npm run e2e-fake",
    "selftest": "node selftest.mjs",
    "calltest": "node calltest.mjs",
    "orchestration-test": "node orchestration-test.mjs",
    "state-advance-policy-test": "node state-advance-policy-test.mjs",
    "bridge-sync-test": "node bridge-sync-test.mjs",
    "bridge-sync-lock-test": "node bridge-sync-lock-test.mjs",
    "python-syntax-test": "python -m py_compile mcp-call.py config-launch-test.py",
    "no-mouse-test": "powershell -ExecutionPolicy Bypass -File verify-no-mouse-policy.ps1",
    "script-syntax-test": "powershell -NoProfile -ExecutionPolicy Bypass -File verify-script-syntax.ps1",
    "proof-script-help-test": "powershell -NoProfile -ExecutionPolicy Bypass -File verify-proof-script-help.ps1",
    "e2e-fake": "node verify-end-to-end-fake.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
